/**
 * Exact enrollment of source-authority contracts with executable rendered-component proof.
 * The blocking test suite reconciles this list with both the contract registry and its exercised
 * identity/state scenarios, so adding a contract cannot silently omit end-to-end render evidence.
 */
export const SOURCE_AUTHORITY_RENDER_PROOF_IDS = Object.freeze([
  "advisor-book-portfolios",
  "risk-mandate-comparison",
]);
